name: CD

on:
  push:
    branches:
      - release-versioning
      #- main
  workflow_dispatch:

permissions:
  contents: write
  packages: read

jobs:
  build:
    name: Continuous Deployment
    uses: ./.github/workflows/release-beta.yaml
